<p style="color: green"><%= notice %></p>

<%= render @post %>

<h2>Comments</h2>
<%= render @post.comments %>

<h2>Add a comment:</h2>
<%= render "comments/form" %>

<br />

<div>
  <%= link_to "Edit this post", edit_post_path(@post) %> |
  <%= link_to "Back to posts", posts_path %>

  <%= button_to "Destroy this post", @post, method: :delete, :class => "rounded-[10px] bg-red-500 hover:bg-red-400 px-3 py-2 mt-5" %>
</div>
